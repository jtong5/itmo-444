The difference between a Docker image and a Docker container is that an image is like a template while a container is a running instance that came from the template. In other words, the image is similar to a blueprint that has everything needed to run an application. The container is the one that runs the process that is created from the blueprint. Additionally, Docker images are read only while Docker containers are mutable.